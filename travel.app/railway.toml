[build]
builder = "python"
root = "travel.app/server-python"
buildCommand = "pip install -r requirements.txt"
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120"

[deploy]
numReplicas = 1
